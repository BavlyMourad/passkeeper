class SizeConfig {
  SizeConfig._();

  // Device type thresholds
  static const double tabletBreakPoint = 800.0;

  // Reference widths for font scaling math
  static const double mobileScalePoint = 360.0;
  static const double tabletScalePoint = 744.0;
}
